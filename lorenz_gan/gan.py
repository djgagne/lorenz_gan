from keras.layers import Input, Conv1D, Dense, Activation, Dropout
import numpy as np

def generator_conv(num_cond_inputs, num_random, num_outputs, activation):
    return

def generator_dense(num_cond_inputs, num_random, num_hidden, num_outputs, activation): 
    return 
